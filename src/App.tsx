import { Button } from "./components/ui/button";
import { Camera } from "lucide-react";

function App() {
	return (
		<div>
			TailwindCSS 적용
			<Button type="button">버튼</Button>
			<Camera />
		</div>
	);
}

export default App;
